# Current Phase

- Objective: M16 (FDC) CLOSED, tagged v1.0.16. M17 (FM-PAC/YM2413 register-accurate device) CLOSED, tagged v1.0.17. M18 (peripheral I/O) CLOSED, tagged v1.0.18. Per the human directive (2026-07-07): "go ahead and kick off M17 and M18 until QA sign-off. Ensure to review deferred items and have them properly addressed during the development," followed by "close M17 and M18. commit, and tag seperately." Both directives are now fully discharged. Order: M11..M16 done -> M17 CLOSED -> M18 CLOSED -> awaiting the human's next directive.
- Active Phase: M18 CLOSED (DEC-0014 / REQ-M18-003), tagged v1.0.18, committed separately from M17 per the human's explicit instruction. QA independently reproduced ctest 79/79, verified the Kanji device is genuinely the direct-port `MSXKanji` pattern (not `MSXKanji12`), verified the printer is genuinely wired at the accurate `#90-#97` claim, confirmed zero edits to `ppi_8255.{h,cpp}` and to `step_cpu_instruction()`/`run_cycles()`/`run_frame()`, confirmed the `JoystickPorts` regression guard is genuine and additive, and independently reproduced the Kanji content-parity claim three separate ways (own SHA1 check, direct file byte spot-check, and a live WSL openMSX 19.1 Tcl probe). Closes deferred-backlog rows B5 and C7; new rows F1/F2 added. Both M17 and M18 are now fully closed, committed, and tagged — eight milestones (M11-M18) tagged in total (v1.0.11..v1.0.18).
- Phase Owner: MSX Master Agent (coordinator)
- Phase Status: M16 closed + tagged v1.0.16. M17 CLOSED + tagged v1.0.17 (DEC-0013). M18 CLOSED + tagged v1.0.18 (DEC-0014). No milestone currently active — awaiting the human's next directive (indicative next per DEC-0009: M19 cartridges, M20 Halnote, or any priority the human specifies).
- Entry Criteria (next milestone, not yet opened): M18 Done (met).
- Exit Criteria: N/A — no milestone currently active.
- Open Blockers: None. Watch-items carried forward for future milestones: (1) C5 (FDC full-boot residual, from M16) — real auto-disk-boot trigger still needs investigation; (2) whichever future milestone implements B6/Halnote must re-verify M17's `BatteryBackedSram` primitive against the real slot-0-3 SRAM window before wiring it in; (3) E1/E2 (YM2413 DSP depth, write-timing) and F1/F2 (cassette tape-format depth, printer rendering depth) are all committed-but-deferred scope, candidate-owned to future audio/rendering milestones paired with C9 (SDL3 frontend); (4) a pre-existing, unrelated M15 residual noted by the M18 planner: the RTC's XML claim is `#B4-#B7` but only `#B4/#B5` are currently wired — not a defect from any closed milestone, not yet actioned.
- Updated At: 2026-07-07T12:30:00+09:00
