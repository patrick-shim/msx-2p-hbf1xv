# Current Phase

- Objective: M16 (FDC) CLOSED, tagged v1.0.16. M17 (FM-PAC/YM2413 register-accurate device) CLOSED, tagged v1.0.17. Per the human directive (2026-07-07): "go ahead and kick off M17 and M18 until QA sign-off. Ensure to review deferred items and have them properly addressed during the development," followed by "close M17 and M18. commit, and tag seperately." Order: M11..M16 done -> M17 CLOSED -> M18 (implemented, QA-passed, closed in a separate subsequent commit per the human's explicit "commit, and tag seperately" instruction).
- Active Phase: M17 CLOSED (DEC-0013 / REQ-M17-003), tagged v1.0.17. QA independently re-verified: device-identity correctness (genuine `MSXMusic`-style, zero `MSXFmPac`-style code), register/latch semantics against `YM2413Okazaki.cc`, `fmmusic_rom_` regression guard byte-for-byte unchanged, `BatteryBackedSram` confirmed NOT wired (per DEC-0012), and independently reproduced the full openMSX A/B harness end-to-end against real WSL openMSX 19.1 with an extra spot-check. ctest 75/75 QA-reproduced, zero regression. Closes backlog B3; B4 correctly left OPEN/re-owned to B6 (not force-closed); E1/E2 added. M18 (peripheral I/O) is implemented and QA-passed on the same evidence-gate discipline, and is being closed and tagged (v1.0.18) in a separate, subsequent commit per the human's explicit instruction to commit and tag the two milestones separately.
- Phase Owner: MSX Master Agent (coordinator)
- Phase Status: M16 closed + tagged v1.0.16. M17 CLOSED + tagged v1.0.17 (this commit). M18 QA-passed, closing next.
- Entry Criteria (M17 closure): QA PASS (met, RESP-M17-003).
- Exit Criteria (M17 closure): Milestone marked Done, `definition-of-done.yaml` overall_done: true, annotated tag v1.0.17 — all met, this commit.
- Open Blockers: None. Watch-items carried forward: (1) whichever future milestone implements B6/Halnote must re-verify `BatteryBackedSram`'s load/save discipline against the real slot-0-3 SRAM window before wiring it in; (2) the YM2413 ROM instrument patch table's provenance caveat must be re-verified against `YM2413NukeYKT.cc` before any future audio-affecting claim (E1).
- Updated At: 2026-07-07T12:20:00+09:00
