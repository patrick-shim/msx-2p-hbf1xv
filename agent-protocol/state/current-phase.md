# Current Phase

- Objective: M22 (Sprites + VDP Command Engine) CLOSED, tagged v1.0.22. Backlog rows D2/D3 DONE (M22); D7 closes IN FULL (no longer partial). Now driving M23 = exact cycle timing (C1/C2/D4) — the THIRD AND FINAL milestone in the pre-authorized run per the human directive (2026-07-07) "let's do M21 - M23 (separate tags each, with qa sign off on each, and of course full system integration test each) like we run autopilot until m23. we need deliberate cross check across the entire system as we go." Order: M11..M22 done -> M23 (next, kickoff pending).
- Phase Owner: MSX Master Agent (coordinator)
- Phase Status: M22 closed by coordinator release decision (2026-07-07/08, DEC-0019/REQ-M22-004), acting on the human's standing pre-authorization. Twelve milestones M11-M22 now tagged (v1.0.11..v1.0.22). Pre-authorization remains in force for M23 (the final milestone of this run): normal human-release-decision gate applies, but the coordinator proceeds through the release-decision/tag step without an additional pause request — UNLESS QA does not reach a clean PASS, in which case: STOP and consult the human. "Deliberate cross-check across the entire system" required at every phase. Current full suite: 117/117 (`ctest`), zero regression through M22.
- M22 summary: `SpriteEngine` (D2) and `VdpCommandEngine` (D3), both owned inside `V9958Vdp` (mirrors M21's `blink_countdown_` ownership style); sprite compositing folds additively into `VdpFrameRenderer::render_frame()` (no new output type). Sprite Mode 1 (4/line) and Mode 2 (8/line) byte-exact per `SpriteChecker.cc/.hh`: EC=bit7/CC=bit6/IC=bit5 (IC excludes collision ONLY, never suppresses rendering — dual-tested); color-0 sprite transparency conditioned on R#8 TP (a genuine fact-sheet-vs-source discrepancy resolved in favor of the source, which itself carries a "verified on real V9958" comment); the 1-pixel vertical shift; S#0 read clears only bits7/6/5. VDP command engine: full R#32-46 register file, all 13 commands via a HYBRID execution model (10 atomic; LMCM/LMMC/HMMC event-driven, mirroring the M16 FDC DRQ/INTRQ precedent — collapsing them to atomic would silently drop data). D7 (G6/G7 planar interleave) closes IN FULL: five new coordinate-address functions, confirmed byte-exact against `VDPCmdEngine.cc:175-410` and confirmed to NOT reference R#2 (commands address both bitmap pages directly through the Y-coordinate's own range, bypassing the display path's page-select register entirely — a genuinely new, independently-discovered nuance). `effective_address()` confirmed genuinely unchanged. A self-caught regression (a sprite-enable gate needed to prevent phantom sprites on freshly-reset VRAM) was found and fixed during implementation. `ctest` 117/117 (106 prior + 11 new), zero regression, independently reproduced by both the coordinator and QA. Real openMSX A/B: raw VRAM-byte comparison achieved genuine PARITY on all 7 probes; several STATUS-register-field divergences were honestly disclosed — QA independently hand-verified these, corrected the developer's specific causal narrative to a more defensible one ("this A/B harness's live-BIOS-driven reference snapshot cannot be guaranteed to reflect the identical injected scenario," not "a stale field survives a read"), and confirmed via algorithm comparison + the full test suite that this is a harness artifact, not a code defect. QA PASS (`docs/m22-qa-signoff.md`), independently surfacing two additional non-blocking documentation-precision findings the implementation report itself had not disclosed.
- M21 summary (prior milestone): First pixel-rendering output path. `VdpFrameRenderer`/`FrameBuffer` — deterministic, pull-model, frozen-register-snapshot renderer, RGB555 pixels, zero new clock consumer. Every Target-Spec mode byte-exact; YJK/YJK+YAE decode byte-exact (incl. an independently-verified floor-vs-truncation finding); GRAPHIC7 GGGRRRBB byte layout; G6/G7 planar interleave's CPU-port + display-path pieces closed (command-engine piece carried to M22, above). `ctest` 106/106, zero regression. QA PASS (`docs/m21-qa-signoff.md`).
- Standing watch-items (carried forward, none blocking): (1) `references/zexall/` (legally-sourced ZEXALL/ZEXDOC suite) remains present but uncommitted, unblocking backlog C3 whenever that milestone is tackled; (2) C5 (FDC full-boot residual) still needs the real auto-disk-boot trigger investigated; (3) any test asserting real ROM/asset content must call `machine.set_asset_root(SONY_MSX_BIOS_DIR)` before `cold_boot()` with the matching `tests/CMakeLists.txt` compile-definition wiring, or it silently 0xFF-fills under `ctest` (DEC-0016); (4) M20's disclosed sub-mapper-shadow A/B divergence (openMSX 19.1 runtime-side) could be re-checked against a newer openMSX build if available — not blocking; (5) the interlace/EO hedge (D6, M21) is a documented SIMPLIFICATION of openMSX's own `getEvenOddMask()`, not a bit-for-bit port — M23 must not assume byte-for-byte formula parity was already achieved here; (6) M21's A/B evidence could not live-compare computed pixel/color values (no introspection point in openMSX) — an environment limitation; (7) NEW from M22: the QA-corrected A/B divergence narrative ("harness cannot guarantee the B-side snapshot reflects the identical injected scenario") is the authoritative characterization going forward, not the original implementation report's "stale-field-survives-a-read" framing; (8) NEW from M22: ASX (S#8/S#9) persistence is narrower in scope than `vdp_command_engine.h`'s own header comment states (only SRCH/LMCM persist it, not LINE/LMMM/HMMM) — a documentation-only correction recommended for a future cycle; (9) NEW from M22: future A/B probes for this device should explicitly include S#7/S#8/S#9 in their comparison gate or explicitly disclose the exclusion.
- Next: kick off M23 = exact cycle timing (backlog C1, C2, D4) — the LAST milestone of this pre-authorized run. This is architecturally the biggest departure yet: M21/M22 both explicitly built PULL-MODEL, frozen-snapshot/atomic devices specifically BECAUSE cycle-accurate timing was deferred to this milestone. M23 must introduce genuine VDP access-slot/cycle-cost modeling (fact-sheet §2/§7/§8: 1368 VDP-cycles/line, slot tables 154/88/31, 16-cycle request latency, CPU-access priority, the ~29-cycle safe-access gap) without breaking the M9/M12 CPU-timing oracles or any of the now-117 deterministic tests built on the pull-model assumption. Also closes C2 (Z80 HALT-R increment, DEC-0004) and C1 (exact cycle/T-state timing parity generally). Grounded in `references/openmsx-21.0/src/video/VDPAccessSlots.{hh,cc}` and the Zilog Z80A fact-sheet §6/§8.
- Indicative follow-on order beyond this run (per `agent-protocol/state/deferred-backlog.md`): M24 ZEXALL (C3) · M25 Sony speed-controller/hardware PAUSE (C8) · M26 SDL3 frontend (C9). Not yet kicked off.
- Updated At: 2026-07-08T00:15:00+09:00
