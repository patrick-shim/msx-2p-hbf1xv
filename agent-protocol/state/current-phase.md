# Current Phase

- Objective: M16 (FDC) CLOSED, tagged v1.0.16. M17 (FM-PAC) CLOSED, tagged v1.0.17. M18 (peripheral I/O) CLOSED, tagged v1.0.18. M19 (cartridge loading) CLOSED, tagged v1.0.19. Order: M11..M19 done -> awaiting the human's next directive.
- Active Phase: M19 CLOSED (DEC-0015 / REQ-M19-003), tagged v1.0.19. Two implementation rounds: (1) core cartridge-loading system — six MVP mapper types (`Mirrored`, `Generic8kB`, `Generic16kB`, `Ascii8kB`, `Ascii16kB`, `Konami` no-SCC) sharing a `CartridgeRomWindow` primitive grounded byte-exact in `RomBlocks::setRom`, wired at primary slots 1 & 2 (unexpanded), a `--cartN`/`--cartN-type` CLI, and `roms/aleste.rom` as a non-identity-claiming `Generic8kB` mechanical smoke fixture; (2) a follow-up (per the human noting a newly-added `roms/metalgear.rom`) that identified it as a genuine Konami-mapper "Metal Gear" (1987) dump via an exact SHA1 match to `references/openmsx-21.0/share/softwaredb.xml`, independently reproduced by the coordinator and QA, plus live WSL openMSX corroboration. QA independently re-verified the bank-resolution mask fallback-only semantics and the Konami mirror-quirk (slots 0/2 permanently fixed at reset; 1/3, 4/6, 5/7 are live mirror pairs — a genuine correction the developer found and fixed relative to an imprecise task-brief shorthand) directly against `RomBlocks.cc`/`RomKonami.cc`. One non-blocking doc-comment staleness found by QA was fixed by the coordinator post-QA (comment-only, zero behavior/test impact, ctest re-confirmed 92/92 after the fix). Real openMSX A/B: content-bearing empty diff with empirically-verified slot-lettering. ctest 92/92, zero regression. Closes backlog B7; new rows G1-G4 added under Section F, all OPEN. Nine milestones (M11-M19) now tagged (v1.0.11..v1.0.19).
- Phase Owner: MSX Master Agent (coordinator)
- Phase Status: No milestone currently active — awaiting the human's next directive (indicative next per DEC-0009: M20 Halnote/MSX-JE firmware, or any priority the human specifies).
- Entry Criteria (next milestone, not yet opened): M19 Done (met).
- Exit Criteria: N/A — no milestone currently active.
- Open Blockers: None. Watch-items carried forward for future milestones: (1) G1 (KonamiSCC + embedded SCC sound chip) is a genuinely new audio device, candidate-paired with E1/C9; (2) G2 (ROM-database/auto-detection), G3 (runtime hot-plug), G4 (long-tail mapper types) are all committed-but-deferred, only worth building if a real need arises; (3) whichever future milestone implements B6/Halnote must re-verify M17's `BatteryBackedSram` primitive against the real slot-0-3 SRAM window before wiring it in; (4) a genuine, valuable discovery unrelated to M19: `references/zexall/` (a legally-sourced, GPL-licensed ZEXALL/ZEXDOC Z80 exerciser suite) is present in the repo — this directly unblocks backlog **C3** (full ZEXDOC/ZEXALL parity sweep, previously blocked since M12), flagged for the human's awareness, not yet acted on; (5) C5 (FDC full-boot residual, from M16) still needs the real auto-disk-boot trigger investigated.
- Updated At: 2026-07-07T14:30:00+09:00
