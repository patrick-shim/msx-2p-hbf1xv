# Current Phase

- Objective: M20 (Halnote/MSX-JE firmware mapping, slot 0-3 + real `BatteryBackedSram` SRAM consumer) CLOSED, tagged v1.0.20. Backlog rows B4 (MSX-JE 16KB SRAM) and B6 (Halnote/MSX-JE firmware mapping) both DONE (M20), closed together per the human's explicit directive. Order: M11..M20 done. No milestone currently active — awaiting the human's next directive.
- Phase Owner: MSX Master Agent (coordinator)
- Phase Status: M20 closed by human release decision (2026-07-07, "let's close it with a proper tag"), DEC-0017/REQ-M20-004. Ten milestones M11-M20 now tagged (v1.0.11..v1.0.20).
- M20 summary: `HalnoteRom` (`src/devices/halnote/halnote_rom.{h,cpp}`) — byte-exact port of `references/openmsx-21.0/src/memory/RomHalnote.{hh,cc}`, reusing `devices::cartridge::CartridgeRomWindow` (M19) and `devices::memory::BatteryBackedSram` (M17) verbatim (neither reimplemented). Main 8-slot window with 4 bank-switch registers (window-slots 2-5), the bit-0x80 double-duty effect preserved exactly (a single write both toggles an enable flag and updates that window-slot's visible bank via the unmasked raw byte); real 16KB SRAM gated at 0x0000-0x3FFF; sub-mapper-enable gate + 2 sub-bank registers + the 0x7000-0x7FFF shadow-read override scoped precisely away from 0x6000-0x6FFF; window-slots 6/7 permanently unmapped. Wired at primary slot 0, secondary slot 3, all 4 CPU pages, reading the real `bios/f1xvfirm.rom`. `ctest` 95/95 (92 prior M1-M19 + 3 new), zero regression, independently reproduced by both the coordinator and QA from clean rebuilds. Real openMSX A/B evidence: 11/14 labels PARITY, 3/14 disclosed non-blocking DIVERGENCE (sub-mapper-shadow read, isolated to the installed openMSX 19.1 runtime) — QA independently re-ran the live probe itself and traced the divergence to reference-runtime behavior via raw byte-level analysis (not a defect in this project's code). QA PASS (`docs/m20-qa-signoff.md`).
- Bonus fix along the way: a genuine test-harness bug was found and fixed in the already-closed, tagged M17 test suite (`FmMusicRom_Slot33Page1_UnchangedByYm2413Writes`'s `#A8`/`#FFFF` slot-routing silently resolved to RAM instead of the FM-MUSIC ROM, plus a silent missing `SONY_MSX_BIOS_DIR` asset-root wiring gap that made the guard's ROM-content assertions vacuous under `ctest`) — test-file-only, zero production risk, logged as DEC-0016, `ctest` reconfirmed 92/92 before M20 implementation began. No re-tag of M17 (no behavior/shipped-code change).
- Standing watch-items (carried forward, none blocking): (1) `references/zexall/` (legally-sourced ZEXALL/ZEXDOC suite) remains present but uncommitted, unblocking backlog C3 whenever that milestone is tackled; (2) C5 (FDC full-boot residual) still needs the real auto-disk-boot trigger investigated; (3) any FUTURE test asserting real ROM/asset content must call `machine.set_asset_root(SONY_MSX_BIOS_DIR)` before `cold_boot()` and have the matching `target_compile_definitions(... SONY_MSX_BIOS_DIR ...)` in `tests/CMakeLists.txt` — `ctest`'s default working directory is the test binary's own build dir, not the repo root, so the relative `"bios"` default silently 0xFF-fills otherwise (see DEC-0016); (4) if a newer openMSX build becomes available in a future environment, re-running `tools/openmsx-m20-halnote-parity.ps1` against it would be a low-cost, valuable confirmatory action for the disclosed sub-mapper-shadow divergence — not a blocking requirement.
- Indicative follow-on order (per `agent-protocol/state/deferred-backlog.md`): M21 VDP rendering depth (D1/D5/D6/D7) · M22 sprites/command engine (D2/D3) · M23 exact cycle timing (C1/C2/D4) · M24 ZEXALL (C3) · M25 Sony speed-controller/hardware PAUSE (C8) · M26 SDL3 frontend (C9). None kicked off yet — awaiting human directive.
- Updated At: 2026-07-07T17:30:00+09:00
