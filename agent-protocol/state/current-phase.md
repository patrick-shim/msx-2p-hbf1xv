# Current Phase

- Objective: M21 (VDP rendering depth) CLOSED, tagged v1.0.21. Backlog rows D1/D5/D6 DONE (M21); D7 advanced to IN-PROGRESS (M21 partial), command-engine piece carried to M22. Now driving M22 = sprites + command engine (D2/D3, plus D7's remainder), part of the pre-authorized three-milestone run per the human directive (2026-07-07) "let's do M21 - M23 (separate tags each, with qa sign off on each, and of course full system integration test each) like we run autopilot until m23. we need deliberate cross check across the entire system as we go." Order: M11..M21 done -> M22 (next, kickoff pending) -> M23.
- Phase Owner: MSX Master Agent (coordinator)
- Phase Status: M21 closed by coordinator release decision (2026-07-07, DEC-0018/REQ-M21-004), acting on the human's standing pre-authorization for this specific M21-M23 run (QA reached a clean PASS, no blocker). Eleven milestones M11-M21 now tagged (v1.0.11..v1.0.21). Pre-authorization remains in force for M22 and M23: normal human-release-decision gate applies to each, but the coordinator proceeds through the release-decision/tag step for each without an additional pause request — UNLESS QA does not reach a clean PASS, in which case: STOP and consult the human before proceeding further. "Deliberate cross-check across the entire system" required at every phase: regression verification is not limited to the new device each cycle, independently confirm no earlier milestone's device/test/golden is disturbed. Current full suite: 106/106 (`ctest`), zero regression through M21.
- M21 summary: First pixel-rendering output path for the emulator. `VdpFrameRenderer`/`FrameBuffer` (`src/devices/video/`) — deterministic, pull-model, frozen-register-snapshot renderer, RGB555 pixels, zero new clock consumer, built purely over the existing M14 `V9958Vdp` port contract (no SDL3 dependency, C9/M26 still unbuilt). Every Target-Spec mode byte-exact (TEXT1/2, GRAPHIC1-7, MULTICOLOR — real 256-wide canvas, not the fact-sheet's 64x48 cell-grid figure; TEXT1Q/MULTIQ/Unknown correctly flat-blank). YJK/YJK+YAE decode byte-exact, including an independently-verified (by developer, QA, and coordinator) finding that the B-channel's floor-vs-truncation rounding risk is real at the pre-clamp level but never observable in the final clamped output. GRAPHIC7's fixed-color byte layout confirmed GGGRRRBB (green in the top 3 bits), not the naively-expected RRRGGGBB. G6/G7 planar interleave (D7) is a 17-bit rotate-right-by-1; M21 closed the CPU-port piece (single surgical edit to M14's `effective_address()`, confirmed not to touch the R#14-carry logic) and the display-path piece; the command-engine piece carries to M22. Scroll/blink/border-mask/multi-page-scroll effects delivered; interlace/EO honestly hedged to an openMSX-parity bar (matching openMSX's own disclosed uncertainty); superimpose correctly scoped N/A (no digitizer hardware). The developer independently caught and fixed a real bug in a first draft of the interlace Field-selection logic during test-writing. `ctest` 106/106 (95 prior + 11 new), zero regression, independently reproduced by coordinator and QA from clean rebuilds. Real openMSX A/B: 4/4 probes raw VRAM/palette/register ARCHITECTURAL PARITY (incl. direct live confirmation of the D7 planar transform's bank1 placement); computed-pixel-color sub-claim honestly BLOCKED (no introspection point in openMSX). QA PASS (`docs/m21-qa-signoff.md`), including independent hand-verification of the YJK math and the interlace-bug fix.
- Standing watch-items (carried forward, none blocking): (1) `references/zexall/` (legally-sourced ZEXALL/ZEXDOC suite) remains present but uncommitted, unblocking backlog C3 whenever that milestone is tackled; (2) C5 (FDC full-boot residual) still needs the real auto-disk-boot trigger investigated; (3) any test asserting real ROM/asset content must call `machine.set_asset_root(SONY_MSX_BIOS_DIR)` before `cold_boot()` with the matching `tests/CMakeLists.txt` compile-definition wiring, or it silently 0xFF-fills under `ctest` (DEC-0016); (4) M20's disclosed sub-mapper-shadow A/B divergence (openMSX 19.1 runtime-side, not this project's code) could be re-checked against a newer openMSX build if one becomes available — not blocking; (5) NEW from M21: the interlace/EO hedge (D6) is a documented SIMPLIFICATION of openMSX's own `getEvenOddMask()`, not a bit-for-bit port — M22/M23 must not assume byte-for-byte formula parity was already achieved here if deeper per-scanline/interlace work is later undertaken; (6) M21's A/B evidence could not live-compare computed pixel/color values (openMSX exposes no such Tcl introspection point for this machine) — an environment limitation, not a code defect.
- Next: kick off M22 = sprites (D2) + VDP command engine (D3), which must ALSO close D7's remaining command-engine-path piece (per DEC-0018's explicit carry-forward). Grounded in `references/openmsx-21.0/src/video/{SpriteChecker,VDPCmdEngine}.{hh,cc}`. Planner must design this atop M21's `VdpFrameRenderer`/`FrameBuffer` architecture (sprite compositing onto the existing background-plane output; command-engine VRAM writes need to interoperate with the same planar-interleave transform D7 already established).
- Indicative follow-on order beyond this run (per `agent-protocol/state/deferred-backlog.md`): M24 ZEXALL (C3) · M25 Sony speed-controller/hardware PAUSE (C8) · M26 SDL3 frontend (C9). Not yet kicked off.
- Updated At: 2026-07-07T21:15:00+09:00
